import styled from '@emotion/styled';
import { CardActions } from '@mui/material';

const MuiCardActionsStyled = styled(CardActions)`
  justify-content: space-between;
  padding: 0;
  padding-bottom: 5px;
`;

export default MuiCardActionsStyled;
